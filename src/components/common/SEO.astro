---
/**
 * SEO Component
 * Enhanced meta tags for social sharing (Open Graph, X Cards)
 */
interface Props {
    title: string;
    description: string;
    image?: string;
    imageAlt?: string;
    type?: "website" | "article" | "profile";
    siteName?: string;
    xHandle?: string;
    article?: {
        publishedTime?: string;
        author?: string;
        tags?: string[];
    };
}

const {
    title,
    description,
    image = "/og-image.jpg",
    imageAlt = "Arafat Hasan - Software Engineer",
    type = "website",
    siteName = "Arafat Hasan",
    xHandle,
    article,
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const imageURL = new URL(image, Astro.site);
---

<!-- Open Graph / Facebook -->
<meta property="og:type" content={type} />
<meta property="og:url" content={canonicalURL} />
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:image" content={imageURL} />
<meta property="og:image:alt" content={imageAlt} />
<meta property="og:image:width" content="1200" />
<meta property="og:image:height" content="630" />
<meta property="og:site_name" content={siteName} />
<meta property="og:locale" content="en_US" />

<!-- X -->
<meta property="x:card" content="summary_large_image" />
<meta property="x:url" content={canonicalURL} />
<meta property="x:title" content={title} />
<meta property="x:description" content={description} />
<meta property="x:image" content={imageURL} />
<meta property="x:image:alt" content={imageAlt} />
{xHandle && <meta property="x:site" content={xHandle} />}
{xHandle && <meta property="x:creator" content={xHandle} />}

<!-- Article specific meta tags -->
{
    article && type === "article" && (
        <>
            {article.publishedTime && (
                <meta
                    property="article:published_time"
                    content={article.publishedTime}
                />
            )}
            {article.author && (
                <meta property="article:author" content={article.author} />
            )}
            {article.tags &&
                article.tags.map((tag) => (
                    <meta property="article:tag" content={tag} />
                ))}
        </>
    )
}
